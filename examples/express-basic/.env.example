# x402charity server URL (deploy your own or use the demo)
X402_SERVER_URL=http://localhost:3402

# Optional: override default charity
CHARITY_ID=testing-charity

# Optional: override default donation amount
DONATION_AMOUNT=$0.001

# Network: base-sepolia (testnet) or base (mainnet)
NETWORK=base-sepolia
